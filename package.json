{
  "name": "anthropair",
  "version": "2026.2.6",
  "type": "module",
  "bin": {
    "anthropair": "./bin/cli.js"
  },
  "files": [
    "bin/",
    "server/",
    "dist/",
    "README.md"
  ],
  "scripts": {
    "prepare": "vite build || true",
    "dev": "node server/index.js",
    "build": "vite build",
    "preview": "vite preview",
    "start": "NODE_ENV=production node server/index.js",
    "prepublishOnly": "vite build",
    "release": "npm version patch && git push && git push --tags && npm publish"
  },
  "dependencies": {
    "@anthropic-ai/claude-code": "^1.0.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "ignore": "^7.0.3",
    "livekit-server-sdk": "^2.9.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "livekit-client": "^2.9.4",
    "vite": "^6.1.0",
    "vitest": "^4.0.18"
  }
}
